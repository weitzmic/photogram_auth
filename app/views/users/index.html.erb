<div class="page-header">
  <h1>
Users
    <a href="/users/new" class="btn btn-lg btn-success">New Photo</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>User</th>
      </tr>

      <% @user.each do |user| %>
      <tr>
        <td><%= user.username %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
